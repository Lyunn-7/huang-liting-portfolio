# dependencies
node_modules

# build output
dist
dist-ssr

# vite cache
.vite

# local env / os
*.local
.DS_Store
*.log

# redundant nested copy (accidental cp -R artifact, not part of this site)
mindloop-landing

# vite temp artifacts
vite.config.ts.timestamp-*.mjs
